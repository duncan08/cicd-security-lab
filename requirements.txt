# Runtime dependencies for the Lambda function itself.
# Currently empty — the handler only uses the Python standard library.
# Keeping this separate from requirements-dev.txt matters for later stages:
# Trivy's dependency/SCA scan in Stage 3 should scan what actually ships
# to production, not our local test tooling.
